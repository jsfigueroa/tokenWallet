export const addressbook = (state = [], action) => {
  switch (action.type) {
    case 'ADD_PUBLIC_KEY':
      return state
    case 'REMOVE_PUBLIC_KEY':
        return state
    default:
      return state
  }
}
